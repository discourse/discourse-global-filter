{{#if @canEditTags}}
  <MiniTagChooser
    @value={{@value}}
    @onChange={{@onChange}}
    @options={{@options}}
    @hiddenFromPreview={{this.hiddenValues}}
  />
  <PopupInputTip @validation={{@tagValidation}} />
{{/if}}
