en:
  site_settings:
    discourse_global_filter_enabled: "Enable the Global Filter plugin"
    global_filters: "The tag(s) that will be the foundation of the global filter system."
    replace_global_filter_with_children: "When building a list of available global filters in the composer, replace a global filter with its filter children if present"
